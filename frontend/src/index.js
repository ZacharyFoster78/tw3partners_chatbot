import React from 'react';
import ReactDOM from 'react-dom/client';
import './Chat.css'; // You might have named this Chat.css
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);